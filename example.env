# Mining Configuration
SOLO_MINING=true
POOL_SERVER=false
MINER_ADDRESS=ObsYourCustomAddress123

# Network Configuration  
TOR_ENABLED=false
SEED_NODES=node1.obsidian.network:8333,node2.obsidian.network:8333

# RPC Configuration
RPC_ADDR=0.0.0.0:8545
EOF && echo "✅ example.env created" && echo "" && echo "# 2. 노드 시작" && echo "docker compose --env-file example.env up -d" && echo "" && echo "# 3. 상태 확인" && echo "docker compose ps" && echo "docker compose logs -f obsidian-node"